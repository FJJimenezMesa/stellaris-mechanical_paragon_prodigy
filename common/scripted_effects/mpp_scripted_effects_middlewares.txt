# ================================================
# Scripted Effects MPP
# ================================================

mpp_middleware_position = {

    inline_script = {
        script = middlewares/mpp_i_s_middleware_position
        MIDDLEWARE = mpp_middleware_call_script
        # ---------- Parámetros del script base ----------
        EFFECT = $EFFECT$
        BASE_SCRIPT = $BASE_SCRIPT$

        # ---------- Lógica de tipología ----------
        TYPE = $TYPE|random$ # !rev
        SIZE = $SIZE|@mpp_random$ # !rev
        # ---------- Lógica de target ----------
        LOCATION = $LOCATION|@mpp_prev$
        LOCATION_TARGET = $LOCATION_TARGET|@mpp_no$
        # ---------- Lógica de distancia ----------
        DISTANCE = $DISTANCE|@mpp_no$
        DISTANCE_OFFSET_RANGE = $DISTANCE_OFFSET_RANGE|@mpp_no$
        MIN_DISTANCE = $MIN_DISTANCE|@mpp_no$
        MAX_DISTANCE = $MAX_DISTANCE|@mpp_no$
        # ---------- Lógica de ángulo  ----------
        ANGLE = $ANGLE|@mpp_no$
        ANGLE_OFFSET_RANGE = $ANGLE_OFFSET_RANGE|@mpp_no$
        MIN_ANGLE = $MIN_ANGLE|@mpp_no$
        MAX_ANGLE = $MAX_ANGLE|@mpp_no$
        # ---------- Lógica de loop (uso en ángulo) ----------
        ITERATION = $ITERATION|1$
        MAX_ITERATIONS = $MAX_ITERATIONS|1$
        # ---------- Otros  ----------
        IN_ORBIT = $IN_ORBIT|@mpp_no$
    }
}

mpp_middleware_call_script = {
    inline_script = {
        script = $BASE_SCRIPT$

        # ---------- Parámetros del script base ----------
        EFFECT = $EFFECT$

        # ---------- Lógica de tipología ----------
        TYPE = $TYPE|random$ # !rev
        SIZE = $SIZE|@mpp_random$ # !rev
        # ---------- Lógica de target ----------
        LOCATION = $LOCATION|@mpp_prev$
        LOCATION_TARGET = $LOCATION_TARGET|@mpp_no$
        # ---------- Lógica de distancia ----------
        DISTANCE = $DISTANCE|@mpp_no$
        DISTANCE_OFFSET_RANGE = $DISTANCE_OFFSET_RANGE|@mpp_no$
        MIN_DISTANCE = $MIN_DISTANCE|@mpp_no$
        MAX_DISTANCE = $MAX_DISTANCE|@mpp_no$
        # ---------- Lógica de ángulo  ----------
        ANGLE = $ANGLE|@mpp_no$
        ANGLE_OFFSET_RANGE = $ANGLE_OFFSET_RANGE|@mpp_no$
        MIN_ANGLE = $MIN_ANGLE|@mpp_no$
        MAX_ANGLE = $MAX_ANGLE|@mpp_no$
        # ---------- Lógica de loop (uso en ángulo) ----------
        ITERATION = $ITERATION|1$
        MAX_ITERATIONS = $MAX_ITERATIONS|1$
        # ---------- Otros  ----------
        IN_ORBIT = $IN_ORBIT|@mpp_no$
        # ---------- Calculados  ----------
        C_MIN_DISTANCE = $C_MIN_DISTANCE|@mpp_random$
        C_MAX_DISTANCE = $C_MAX_DISTANCE|@mpp_random$
        C_MIN_ANGLE = $C_MIN_ANGLE|@mpp_random$
        C_MAX_ANGLE = $C_MAX_ANGLE|@mpp_random$
    }
}