# ===============================================
# Planetas, cinturones de asteroides y asteroides
# ===============================================

mpp_spawn_planet = {
    inline_script = {
        script = "galactic_objects/mpp_i_s_g_o_orbit_offset"
        TYPE = $TYPE|random$
        ORBIT = $ORBIT|random$
        ORBIT_OFFSET = $ORBIT_OFFSET|0$
        SIZE = $SIZE|random$
        MIN_ANGLE = $MIN_ANGLE|0$
        MAX_ANGLE = $MAX_ANGLE|360$
    }

    if = {
        limit = { exists = event_target:tPlanet }
        event_target:tPlanet = {
            [[EFFECT1] planet = { $EFFECT1$ = yes } ]
            [[EFFECT2] planet = { $EFFECT2$ = yes } ]
            [[EFFECT3] planet = { $EFFECT3$ = yes } ]
            [[EFFECT4] planet = { $EFFECT4$ = yes } ]
            [[EFFECT5] planet = { $EFFECT5$ = yes } ]
        }
    }
}

mpp_add_asteroid_belt = {
    # Si el parámetro TYPE está definido → usar ese tipo exacto de cinturón de asteroides
    [[TYPE]
        add_asteroid_belt = {
            type = $TYPE$       # Ejemplo: rocky_asteroid_belt, icy_asteroid_belt, etc.
            radius = $RADIUS$   # Radio del cinturón
        }
    ]

    # Si no se ha definido TYPE → elegir aleatoriamente entre los tipos disponibles
    [[!TYPE]
        random_list = {
            1 = { add_asteroid_belt = { type = rocky_asteroid_belt radius = $RADIUS$ } }
            1 = { add_asteroid_belt = { type = icy_asteroid_belt radius = $RADIUS$ } }
            1 = { add_asteroid_belt = { type = crystal_asteroid_belt radius = $RADIUS$ } }
            1 = { add_asteroid_belt = { type = debris_asteroid_belt radius = $RADIUS$ } }
            1 = { add_asteroid_belt = { type = empty_asteroid_belt radius = $RADIUS$ } }
        }
    ]
}

mpp_add_asteroids = {
    # Si se define COUNT → asignar esa cantidad a la variable temporal tCount
    [[COUNT]
        set_variable = {
            which = tCount
            value = $COUNT|1$    # $COUNT|1$ → si no se pasa COUNT, usa valor por defecto 1
        }
    ]

    # Si NO se define COUNT → generar un número aleatorio entre MIN_COUNT y MAX_COUNT
    [[!COUNT]
        set_variable_to_random_value = {
            which = tCount
            min = $MIN_COUNT|1$  # valor mínimo por defecto = 1
            max = $MAX_COUNT|1$  # valor máximo por defecto = 1 (revisar si debería ser >1)
            rounded = yes        # asegura número entero
        }
    ]

    # Bucle while: ejecuta el bloque interno tCount veces
    while = {
        count = tCount

        # Caso 1: TYPE definido → añade planeta del tipo especificado
        [[TYPE]
            mpp_spawn_planet = {
                TYPE = $TYPE$
                ORBIT = $ORBIT|50$
                ORBIT_OFFSET = $ORBIT_OFFSET|0$
                SIZE = $SIZE|2$
                MIN_ANGLE = $MIN_ANGLE|0$
                MAX_ANGLE = $MAX_ANGLE|360$
                EFFECT1 = mpp_setName_randomPlanet
            }
        ]

        # Caso 2: TYPE no definido → genera un asteroide aleatorio
        [[!TYPE]
            # random_list: escoge un tipo de asteroide con probabilidad igual (peso = 1)
            random_list = {
                1 = {
                    mpp_spawn_planet = {
                        TYPE = pc_asteroid
                        ORBIT = $ORBIT|50$
                        ORBIT_OFFSET = $ORBIT_OFFSET|0$
                        SIZE = $SIZE|2$
                        MIN_ANGLE = $MIN_ANGLE|0$
                        MAX_ANGLE = $MAX_ANGLE|360$
                        EFFECT1 = mpp_setName_randomPlanet
                    }
                }
                1 = {
                    mpp_spawn_planet = {
                        TYPE = pc_ice_asteroid
                        ORBIT = $ORBIT|50$
                        ORBIT_OFFSET = $ORBIT_OFFSET|0$
                        SIZE = $SIZE|2$
                        MIN_ANGLE = $MIN_ANGLE|0$
                        MAX_ANGLE = $MAX_ANGLE|360$
                        EFFECT1 = mpp_setName_randomPlanet
                    }
                }
                1 = {
                    mpp_spawn_planet = {
                        TYPE = pc_crystal_asteroid
                        ORBIT = $ORBIT|50$
                        ORBIT_OFFSET = $ORBIT_OFFSET|0$
                        SIZE = $SIZE|2$
                        MIN_ANGLE = $MIN_ANGLE|0$
                        MAX_ANGLE = $MAX_ANGLE|360$
                        EFFECT1 = mpp_setName_randomPlanet
                    }
                }
                1 = {
                    mpp_spawn_planet = {
                        TYPE = pc_rare_crystal_asteroid
                        ORBIT = $ORBIT|50$
                        ORBIT_OFFSET = $ORBIT_OFFSET|0$
                        SIZE = $SIZE|2$
                        MIN_ANGLE = $MIN_ANGLE|0$
                        MAX_ANGLE = $MAX_ANGLE|360$
                        EFFECT1 = mpp_setName_randomPlanet
                    }
                }
            }
        ]
    }

    # Limpia la variable temporal usada en el bucle
    clear_variable = tCount
}

# ===============================================
# Efectos
# ===============================================

mpp_add_ambient_object = {
    [[IN_ORBIT]
        set_variable = { which = inOrbit value = 1 }
    ]

    [[TARGET]
        if = {
            limit = { is_variable_set = inOrbit }
            create_ambient_object = {
                type = $TYPE|small_debris_object$
                location = event_target:$TARGET$
                entity_offset_height = @inOritOffset
            }
        }
        else = {
            create_ambient_object = {
                type = $TYPE|small_debris_object$
                location = event_target:$TARGET$
            }
        }

    ]
    [[!TARGET]
        if = {
            limit = { is_variable_set = inOrbit }
            create_ambient_object = {
                type = $TYPE|small_debris_object$
                location = prev
                entity_offset_height = @inOritOffset
            }
        }
        else = {
            create_ambient_object = {
                type = $TYPE|small_debris_object$
                location = prev
            }
        }
    ]

    [[DISTANCE]
        last_created_ambient_object = {
            set_location = {
                target = prev # Lo movemos con respecto a su actual localización
                distance = $DISTANCE$
                angle = random
            }
        }
    ]
    [[!DISTANCE]
        last_created_ambient_object = {
            inline_script = {
                script = "galactic_objects/mpp_i_s_g_o_set_location"
                MIN_DISTANCE = $MIN_DISTANCE|0$
                MAX_DISTANCE = $MAX_DISTANCE|5$
            }
        }
    ]

    clear_variable = inOrbit
}

mpp_add_debris = {
    # Si se define COUNT → asignar esa cantidad a la variable temporal tCount
    [[COUNT]
        set_variable = {
            which = tCount
            value = $COUNT|1$    # $COUNT|1$ → si no se pasa COUNT, usa valor por defecto 1
        }
    ]

    # Si NO se define COUNT → generar un número aleatorio entre MIN_COUNT y MAX_COUNT
    [[!COUNT]
        set_variable_to_random_value = {
            which = tCount
            min = $MIN_COUNT|1$  # valor mínimo por defecto = 1
            max = $MAX_COUNT|1$  # valor máximo por defecto = 1 (revisar si debería ser >1)
            rounded = yes        # asegura número entero
        }
    ]

    # Bucle while: ejecuta el bloque interno tCount veces
    while = {
        count = tCount

        # Caso 1: TYPE definido → añade debris del tipo especificado
        [[TYPE]
            mpp_add_ambient_object = {
                TYPE = $TYPE$
                MIN_DISTANCE = $MIN_DISTANCE$
                MAX_DISTANCE = $MAX_DISTANCE$
                IN_ORBIT = $IN_ORBIT|no$
            }
        ]

        # Caso 2: TYPE no definido → genera un debris aleatorio
        [[!TYPE]
            # random_list: escoge un tipo de debris con probabilidad igual (peso = 1)
            random_list = {
                1 = {
                    mpp_add_ambient_object = {
                        TYPE = small_debris_object
                        MIN_DISTANCE = $MIN_DISTANCE$
                        MAX_DISTANCE = $MAX_DISTANCE$
                        IN_ORBIT = $IN_ORBIT|no$
                    }
                }
                1 = {
                    mpp_add_ambient_object = {
                        TYPE = medium_debris_01_object
                        MIN_DISTANCE = $MIN_DISTANCE|10$
                        MAX_DISTANCE = $MAX_DISTANCE|15$
                        IN_ORBIT = $IN_ORBIT|no$
                    }
                }
                1 = {
                    mpp_add_ambient_object = {
                        TYPE = medium_debris_02_object
                        MIN_DISTANCE = $MIN_DISTANCE$
                        MAX_DISTANCE = $MAX_DISTANCE$
                        IN_ORBIT = $IN_ORBIT|no$
                    }
                }
                1 = {
                    mpp_add_ambient_object = {
                        TYPE = large_debris_object
                        MIN_DISTANCE = $MIN_DISTANCE$
                        MAX_DISTANCE = $MAX_DISTANCE$
                        IN_ORBIT = $IN_ORBIT|no$
                    }
                }
            }
        ]

        # Limpia la variable temporal usada en el bucle
        clear_variable = tCount
    }
}

mpp_add_ship_debris = {
    # Si se define COUNT → asignar esa cantidad a la variable temporal tCount
    [[COUNT]
        set_variable = {
            which = tCount
            value = $COUNT|1$    # $COUNT|1$ → si no se pasa COUNT, usa valor por defecto 1
        }
    ]

    # Si NO se define COUNT → generar un número aleatorio entre MIN_COUNT y MAX_COUNT
    [[!COUNT]
        set_variable_to_random_value = {
            which = tCount
            min = $MIN_COUNT|1$  # valor mínimo por defecto = 1
            max = $MAX_COUNT|1$  # valor máximo por defecto = 1 (revisar si debería ser >1)
            rounded = yes        # asegura número entero
        }
    ]

    # Bucle while: ejecuta el bloque interno tCount veces
    while = {
        count = tCount

        # Caso 1: TYPE definido → añade debris del tipo especificado
        [[TYPE]
            mpp_add_ambient_object = {
                TYPE = $TYPE$
                MIN_DISTANCE = $MIN_DISTANCE$
                MAX_DISTANCE = $MAX_DISTANCE$
            }
        ]

        # Caso 2: TYPE no definido → genera un debris aleatorio
        [[!TYPE]
            # random_list: escoge un tipo de debris con probabilidad igual (peso = 1)
            random_list = {
                1 = {
                    mpp_add_ambient_object = {
                        TYPE = abandoned_ship_object
                        MIN_DISTANCE = $MIN_DISTANCE$
                        MAX_DISTANCE = $MAX_DISTANCE$
                    }
                }
                1 = {
                    mpp_add_ambient_object = {
                        TYPE = abandoned_ship_2_object
                        MIN_DISTANCE = $MIN_DISTANCE$
                        MAX_DISTANCE = $MAX_DISTANCE$
                    }
                }
            }
        ]

        # Limpia la variable temporal usada en el bucle
        clear_variable = tCount
    }
}

mpp_add_battlefield_debris = {
    mpp_add_ambient_object = {
        TYPE = ancient_battlefield_object
        MIN_DISTANCE = $MIN_DISTANCE$
        MAX_DISTANCE = $MAX_DISTANCE$
    }

    [[SHIP_DEBRIS]
        last_created_ambient_object = {
            save_event_target_as = tObject
        }

        event_target:tObject = {
            mpp_add_ship_debris = {
                COUNT = $SHIP_DEBRIS$
                MIN_DISTANCE = $SHIP_DEBRIS_MIN_DISTANCE$
                MAX_DISTANCE = $SHIP_DEBRIS_MAX_DISTANCE$
            }
        }
    ]
}

# !Rev hay máximo de 1 por partida...
mpp_add_broken_star_lifting_platform = {
    solar_system = {
        random_system_planet = {
            limit = { is_star = yes }
            mpp_add_ambient_object = {
                TYPE = broken_star_lifting_platform
                DISTANCE = $DISTANCE|20$
            }
            [[ADD_EVENT]
                last_created_ambient_object = {
                    save_global_event_target_as = broken_star_lifting_platform
                }

                prev = {
                    set_star_flag = star_lifting_system
                }

                add_anomaly = {
                    category = star_lifting_category
                }
            ]

            [[ADD_BATTLEFIELD]
                event_target:broken_star_lifting_platform = {
                    mpp_add_battlefield_debris = {
                        MIN_DISTANCE = 5
                        MAX_DISTANCE = 10
                    }
                }
            ]
        }
    }
}


# shrine_object
# abandoned_weapon_station
# droppod_02_object_desc
# habitat_nanobot_object
# ringworld_nanobot_object
#
#
#
#
#
#
#
#
#


# ===============================================
# Eventos
# ===============================================


