set_variable = { which = tLocation value = $LOCATION$ }
set_variable = { which = inOrbit value = $IN_ORBIT$ }

if = {
    limit = { NOT = { check_variable = { which = tLocation value = @mpp_no } } }
    # LOCATION definido
    if = {
        limit = { check_variable = { which = tLocation value = @mpp_prev } }
        # IN ORBIT
        if = {
            limit = { check_variable = { which = inOrbit value = @mpp_yes } }
            set_location = {
                target = prev
                distance = $LOCATION_DISTANCE$
                angle = random  # TODO añadir cálculo de angle
                direction = in_system
            }
            # ---------- Debug ----------
            mpp_log_script_effect = {
                EFFECT = $EFFECT$ SCRIPT = mpp_i_s_set_location
                TEXT = "set_location en prev a $LOCATION_DISTANCE$ in_system"
            }
        }
        else = {
            set_location = {
                target = prev
                distance = $LOCATION_DISTANCE$
                angle = random  # TODO añadir cálculo de angle
                direction = out_system
            }
            # ---------- Debug ----------
            mpp_log_script_effect = {
                EFFECT = $EFFECT$ SCRIPT = mpp_i_s_set_location
                TEXT = "set_location en prev a $LOCATION_DISTANCE$ out_system"
            }
        }
    }
    else_if = {
        limit = { check_variable = { which = tLocation value = @mpp_from } }
        # IN ORBIT
        if = {
            limit = { check_variable = { which = inOrbit value = @mpp_yes } }
            set_location = {
                target = from
                distance = $LOCATION_DISTANCE$
                angle = random  # TODO añadir cálculo de angle
                direction = in_system
            }
            # ---------- Debug ----------
            mpp_log_script_effect = {
                EFFECT = $EFFECT$ SCRIPT = mpp_i_s_set_location
                TEXT = "set_location en from a $LOCATION_DISTANCE$ in_system"
            }
        }
        else = {
            set_location = {
                target = from
                distance = $LOCATION_DISTANCE$
                angle = random  # TODO añadir cálculo de angle
                direction = out_system
            }
            # ---------- Debug ----------
            mpp_log_script_effect = {
                EFFECT = $EFFECT$ SCRIPT = mpp_i_s_set_location
                TEXT = "set_location en from a $LOCATION_DISTANCE$ out_system"
            }
        }
    }
    else_if = {
        limit = { check_variable = { which = tLocation value = @mpp_prev2 } }
        # IN ORBIT
        if = {
            limit = { check_variable = { which = inOrbit value = @mpp_yes } }
            set_location = {
                target = prevprev
                distance = $LOCATION_DISTANCE$
                angle = random  # TODO añadir cálculo de angle
                direction = in_system
            }
            # ---------- Debug ----------
            mpp_log_script_effect = {
                EFFECT = $EFFECT$ SCRIPT = mpp_i_s_set_location
                TEXT = "set_location en prevprev a $LOCATION_DISTANCE$ in_system"
            }
        }
        else = {
            set_location = {
                target = prevprev
                distance = $LOCATION_DISTANCE$
                angle = random  # TODO añadir cálculo de angle
                direction = out_system
            }
            # ---------- Debug ----------
            mpp_log_script_effect = {
                EFFECT = $EFFECT$ SCRIPT = mpp_i_s_set_location
                TEXT = "set_location en prevprev a $LOCATION_DISTANCE$ out_system"
            }
        }
    }
    else = {
        # IN ORBIT
        if = {
            limit = { check_variable = { which = inOrbit value = @mpp_yes } }
            set_location = {
                target = $LOCATION$
                distance = $LOCATION_DISTANCE$
                angle = random  # TODO añadir cálculo de angle
                direction = in_system
            }
            # ---------- Debug ----------
            mpp_log_script_effect = {
                EFFECT = $EFFECT$ SCRIPT = mpp_i_s_set_location
                TEXT = "set_location en $LOCATION$ a $LOCATION_DISTANCE$ in_system"
            }
        }
        else = {
            set_location = {
                target = $LOCATION$
                distance = $LOCATION_DISTANCE$
                angle = random  # TODO añadir cálculo de angle
                direction = out_system
            }
            # ---------- Debug ----------
            mpp_log_script_effect = {
                EFFECT = $EFFECT$ SCRIPT = mpp_i_s_set_location
                TEXT = "set_location en $LOCATION$ a $LOCATION_DISTANCE$ out_system"
            }
        }
    }
}
else_if = {
    limit = { exists = prev }
    # IN ORBIT
    if = {
        limit = { check_variable = { which = inOrbit value = @mpp_yes } }
        set_location = {
            target = prev
            distance = $LOCATION_DISTANCE$
            angle = random  # TODO añadir cálculo de angle
            direction = in_system
        }
        # ---------- Debug ----------
        mpp_log_script_effect = {
            EFFECT = $EFFECT$ SCRIPT = mpp_i_s_set_location
            TEXT = "set_location en prev (default) a $LOCATION_DISTANCE$ in_system"
        }
    }
    else = {
        set_location = {
            target = prev
            distance = $LOCATION_DISTANCE$
            angle = random  # TODO añadir cálculo de angle
            direction = out_system
        }
        # ---------- Debug ----------
        mpp_log_script_effect = {
            EFFECT = $EFFECT$ SCRIPT = mpp_i_s_set_location
            TEXT = "set_location en prev (default) a $LOCATION_DISTANCE$ out_system"
        }
    }
}
else_if = {
    limit = { exists = from }
    # IN ORBIT
    if = {
        limit = { check_variable = { which = inOrbit value = @mpp_yes } }
        set_location = {
            target = from
            distance = $LOCATION_DISTANCE$
            angle = random  # TODO añadir cálculo de angle
            direction = in_system
        }
        # ---------- Debug ----------
        mpp_log_script_effect = {
            EFFECT = $EFFECT$ SCRIPT = mpp_i_s_set_location
            TEXT = "set_location en from a $LOCATION_DISTANCE$ in_system"
        }
    }
    else = {
        set_location = {
            target = from
            distance = $LOCATION_DISTANCE$
            angle = random  # TODO añadir cálculo de angle
            direction = out_system
        }
        # ---------- Debug ----------
        mpp_log_script_effect = {
            EFFECT = $EFFECT$ SCRIPT = mpp_i_s_set_location
            TEXT = "set_location en from (default) a $LOCATION_DISTANCE$ out_system"
        }
    }
}
else = {
    # IN ORBIT
    if = {
        limit = { check_variable = { which = inOrbit value = @mpp_yes } }
        set_location = {
            target = this
            distance = $LOCATION_DISTANCE$
            angle = random  # TODO añadir cálculo de angle
            direction = in_system
        }
        # ---------- Debug ----------
        mpp_log_script_effect = {
            EFFECT = $EFFECT$ SCRIPT = mpp_i_s_set_location
            TEXT = "set_location en this (default) a $LOCATION_DISTANCE$ in_system"
        }
    }
    else = {
        set_location = {
            target = this
            distance = $LOCATION_DISTANCE$
            angle = random  # TODO añadir cálculo de angle
            direction = out_system
        }
        # ---------- Debug ----------
        mpp_log_script_effect = {
            EFFECT = $EFFECT$ SCRIPT = mpp_i_s_set_location
            TEXT = "set_location en this (default) a $LOCATION_DISTANCE$ out_system"
        }
    }
}

# ---------- Limpieza de variables ----------
clear_variable = tLocation
clear_variable = inOrbit