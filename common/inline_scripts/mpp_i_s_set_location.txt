set_variable = { which = tLocationTarget value = tFleetLocation }

if = {
    limit = { check_variable = { which = tLocationTarget value = @mpp_prev } }
    set_location = {
        target = prev
        distance = $DISTANCE$
    }
    # ---------- Debug ----------
    mpp_log_script_config = {
        EFFECT = $EFFECT$ SCRIPT = mpp_i_s_set_location
        TEXT = "set_location en prev a $DISTANCE$"
    }
}
else_if = {
    limit = { check_variable = { which = tLocationTarget value = @mpp_prev2 } }
    set_location = {
        target = prevprev
        distance = $DISTANCE$
    }
    # ---------- Debug ----------
    mpp_log_script_config = {
        EFFECT = $EFFECT$ SCRIPT = mpp_i_s_set_location
        TEXT = "set_location en prevprev a $DISTANCE$"
    }
}
else_if = {
    limit = { check_variable = { which = tLocationTarget value = @mpp_prev3 } }
    set_location = {
        target = prevprevprev
        distance = $DISTANCE$
    }
    # ---------- Debug ----------
    mpp_log_script_config = {
        EFFECT = $EFFECT$ SCRIPT = mpp_i_s_set_location
        TEXT = "set_location en prevprevprev a $DISTANCE$"
    }
}
else_if = {
    limit = { check_variable = { which = tLocationTarget value = @mpp_from } }
    set_location = {
        target = from
        distance = $DISTANCE$
    }
    # ---------- Debug ----------
    mpp_log_script_config = {
        EFFECT = $EFFECT$ SCRIPT = mpp_i_s_set_location
        TEXT = "set_location en from a $DISTANCE$"
    }
}
else_if = {
    limit = { check_variable = { which = tLocationTarget value = @mpp_from2 } }
    set_location = {
        target = fromfrom
        distance = $DISTANCE$
    }
    # ---------- Debug ----------
    mpp_log_script_config = {
        EFFECT = $EFFECT$ SCRIPT = mpp_i_s_set_location
        TEXT = "set_location en fromfrom a $DISTANCE$"
    }
}
else_if = {
    limit = { check_variable = { which = tLocationTarget value = @mpp_from3 } }
    set_location = {
        target = fromfromfrom
        distance = $DISTANCE$
    }
    # ---------- Debug ----------
    mpp_log_script_config = {
        EFFECT = $EFFECT$ SCRIPT = mpp_i_s_set_location
        TEXT = "set_location en fromfromfrom a $DISTANCE$"
    }
}
else_if = {
    limit = { NOT = { check_variable = { which = tLocationTarget value = @mpp_no } } }
    set_location = {
        target = $TARGET$
        distance = $DISTANCE$
    }
    # ---------- Debug ----------
    mpp_log_script_config = {
        EFFECT = $EFFECT$ SCRIPT = mpp_i_s_set_location
        TEXT = "set_location en $TARGET$ a $DISTANCE$"
    }
}

# ---------- Limpieza de variables ----------
clear_variable = tLocationTarget