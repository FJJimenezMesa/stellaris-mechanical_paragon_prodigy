limit = { check_variable_arithmetic = { which = tOffset value = $T_OFFSET$ } }

set_location = {
    target = $TARGET$
    distance = $F_DISTANCE$     # Distancia random final

    angle = random
}

log_error = "base entra $TARGET$"
log_error = "base entra offset \\[this.tOffset]"