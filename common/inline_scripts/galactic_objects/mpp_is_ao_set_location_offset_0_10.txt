if = {
    inline_script = {
        script = galactic_objects/mpp_is_ao_set_location_offset_base TARGET = $TARGET$ F_DISTANCE = @[$DISTANCE$ + $B_OFFSET$ + 0] T_OFFSET = @[$B_OFFSET$ + 0]
    }
}
else_if = {
    inline_script = {
        script = galactic_objects/mpp_is_ao_set_location_offset_base TARGET = $TARGET$ F_DISTANCE = @[$DISTANCE$ + $B_OFFSET$ + 1] T_OFFSET = @[$B_OFFSET$ + 1]
    }
}
else_if = {
    inline_script = {
        script = galactic_objects/mpp_is_ao_set_location_offset_base TARGET = $TARGET$ F_DISTANCE = @[$DISTANCE$ + $B_OFFSET$ + 2] T_OFFSET = @[$B_OFFSET$ + 2]
    }
}
else_if = {
    inline_script = {
        script = galactic_objects/mpp_is_ao_set_location_offset_base TARGET = $TARGET$ F_DISTANCE = @[$DISTANCE$ + $B_OFFSET$ + 3] T_OFFSET = @[$B_OFFSET$ + 3]
    }
}
else_if = {
    inline_script = {
        script = galactic_objects/mpp_is_ao_set_location_offset_base TARGET = $TARGET$ F_DISTANCE = @[$DISTANCE$ + $B_OFFSET$ + 4] T_OFFSET = @[$B_OFFSET$ + 4]
    }
}
else_if = {
    inline_script = {
        script = galactic_objects/mpp_is_ao_set_location_offset_base TARGET = $TARGET$ F_DISTANCE = @[$DISTANCE$ + $B_OFFSET$ + 5] T_OFFSET = @[$B_OFFSET$ + 5]
    }
}
else_if = {
    inline_script = {
        script = galactic_objects/mpp_is_ao_set_location_offset_base TARGET = $TARGET$ F_DISTANCE = @[$DISTANCE$ + $B_OFFSET$ + 6] T_OFFSET = @[$B_OFFSET$ + 6]
    }
}
else_if = {
    inline_script = {
        script = galactic_objects/mpp_is_ao_set_location_offset_base TARGET = $TARGET$ F_DISTANCE = @[$DISTANCE$ + $B_OFFSET$ + 7] T_OFFSET = @[$B_OFFSET$ + 7]
    }
}
else_if = {
    inline_script = {
        script = galactic_objects/mpp_is_ao_set_location_offset_base TARGET = $TARGET$ F_DISTANCE = @[$DISTANCE$ + $B_OFFSET$ + 8] T_OFFSET = @[$B_OFFSET$ + 8]
    }
}
else_if = {
    inline_script = {
        script = galactic_objects/mpp_is_ao_set_location_offset_base TARGET = $TARGET$ F_DISTANCE = @[$DISTANCE$ + $B_OFFSET$ + 9] T_OFFSET = @[$B_OFFSET$ + 9]
    }
}
else_if = {
    inline_script = {
        script = galactic_objects/mpp_is_ao_set_location_offset_base TARGET = $TARGET$ F_DISTANCE = @[$DISTANCE$ + $B_OFFSET$ + 10] T_OFFSET = @[$B_OFFSET$ + 10]
    }
}