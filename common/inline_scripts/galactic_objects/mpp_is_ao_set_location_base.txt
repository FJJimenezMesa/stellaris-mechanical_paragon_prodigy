limit = { check_variable_arithmetic = { which = tDistance value = $F_DISTANCE$ } }

set_location = {
    target = $TARGET$
    distance = $F_DISTANCE$     # Distancia random final
    angle = random
}

log_error = "base entra $TARGET$"
log_error = "base entra distancia \\[this.tDistance]"