if = {
    inline_script = {
        script = galactic_objects/mpp_i_s_go_orbit_offset_spawn
        EFFECT = $EFFECT$ TYPE = $TYPE$ SIZE = $SIZE$ MIN_ANGLE = $MIN_ANGLE$ MAX_ANGLE = $MAX_ANGLE$
        O_OFFSET = @[ $B_OFFSET$ + 0  ]
        F_ORBIT = @[ $ORBIT$ + $B_OFFSET$ + 0 ]
    }
}
else_if = {
    inline_script = {
        script = galactic_objects/mpp_i_s_go_orbit_offset_spawn
        EFFECT = $EFFECT$ TYPE = $TYPE$ SIZE = $SIZE$ MIN_ANGLE = $MIN_ANGLE$ MAX_ANGLE = $MAX_ANGLE$
        O_OFFSET = @[ $B_OFFSET$ + 1 ]  F_ORBIT = @[ $ORBIT$ + $B_OFFSET$ + 1 ] 
    }
}
else_if = {
    inline_script = {
        script = galactic_objects/mpp_i_s_go_orbit_offset_spawn
        EFFECT = $EFFECT$ TYPE = $TYPE$ SIZE = $SIZE$ MIN_ANGLE = $MIN_ANGLE$ MAX_ANGLE = $MAX_ANGLE$
        O_OFFSET = @[ $B_OFFSET$ + 2  ]
        F_ORBIT = @[ $ORBIT$ + $B_OFFSET$ + 2 ]
    }
}
else_if = {
    inline_script = {
        script = galactic_objects/mpp_i_s_go_orbit_offset_spawn
        EFFECT = $EFFECT$ TYPE = $TYPE$ SIZE = $SIZE$ MIN_ANGLE = $MIN_ANGLE$ MAX_ANGLE = $MAX_ANGLE$
        O_OFFSET = @[ $B_OFFSET$ + 3  ]
        F_ORBIT = @[ $ORBIT$ + $B_OFFSET$ + 3 ]
    }
}
else_if = {
    inline_script = {
        script = galactic_objects/mpp_i_s_go_orbit_offset_spawn
        EFFECT = $EFFECT$ TYPE = $TYPE$ SIZE = $SIZE$ MIN_ANGLE = $MIN_ANGLE$ MAX_ANGLE = $MAX_ANGLE$
        O_OFFSET = @[ $B_OFFSET$ + 4  ]
        F_ORBIT = @[ $ORBIT$ + $B_OFFSET$ + 4 ]
    }
}
else_if = {
    inline_script = {
        script = galactic_objects/mpp_i_s_go_orbit_offset_spawn
        EFFECT = $EFFECT$ TYPE = $TYPE$ SIZE = $SIZE$ MIN_ANGLE = $MIN_ANGLE$ MAX_ANGLE = $MAX_ANGLE$
        O_OFFSET = @[ $B_OFFSET$ + 5  ]
        F_ORBIT = @[ $ORBIT$ + $B_OFFSET$ + 5 ]
    }
}
else_if = {
    inline_script = {
        script = galactic_objects/mpp_i_s_go_orbit_offset_spawn
        EFFECT = $EFFECT$ TYPE = $TYPE$ SIZE = $SIZE$ MIN_ANGLE = $MIN_ANGLE$ MAX_ANGLE = $MAX_ANGLE$
        O_OFFSET = @[ $B_OFFSET$ + 6  ]
        F_ORBIT = @[ $ORBIT$ + $B_OFFSET$ + 6 ]
    }
}
else_if = {
    inline_script = {
        script = galactic_objects/mpp_i_s_go_orbit_offset_spawn
        EFFECT = $EFFECT$ TYPE = $TYPE$ SIZE = $SIZE$ MIN_ANGLE = $MIN_ANGLE$ MAX_ANGLE = $MAX_ANGLE$
        O_OFFSET = @[ $B_OFFSET$ + 7  ]
        F_ORBIT = @[ $ORBIT$ + $B_OFFSET$ + 7 ]
    }
}
else_if = {
    inline_script = {
        script = galactic_objects/mpp_i_s_go_orbit_offset_spawn
        EFFECT = $EFFECT$ TYPE = $TYPE$ SIZE = $SIZE$ MIN_ANGLE = $MIN_ANGLE$ MAX_ANGLE = $MAX_ANGLE$
        O_OFFSET = @[ $B_OFFSET$ + 8  ]
        F_ORBIT = @[ $ORBIT$ + $B_OFFSET$ + 8 ]
    }
}
else_if = {
    inline_script = {
        script = galactic_objects/mpp_i_s_go_orbit_offset_spawn
        EFFECT = $EFFECT$ TYPE = $TYPE$ SIZE = $SIZE$ MIN_ANGLE = $MIN_ANGLE$ MAX_ANGLE = $MAX_ANGLE$
        O_OFFSET = @[ $B_OFFSET$ + 9 ] 
        F_ORBIT = @[ $ORBIT$ + $B_OFFSET$ + 9 ]
    }
}

