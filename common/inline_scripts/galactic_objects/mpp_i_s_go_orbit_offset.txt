set_variable = {
    which = tOffsetMax
    value = $ORBIT_OFFSET$
}
set_variable = {
    which = tOffsetMin
    value = tOffsetMax
}
if = {
    limit = { NOT = { check_variable = { which = tOffsetMin value = 0 } } }
    multiply_variable = {
        which = tOffsetMin
        value = -1
    }
}
set_variable_to_random_value = {
    which = tOrbitOffset
    MIN = tOffsetMin
    MAX = tOffsetMax
    rounded = yes
}

if = {
    limit = { check_variable_arithmetic = { which = tOrbitOffset value >= 0 } }
    if = {
        limit = { check_variable_arithmetic = { which = tOrbitOffset value < 10 } }
        inline_script = {
            script = galactic_objects/mpp_i_s_go_orbit_offset_0_10
            B_OFFSET = 0
            TYPE = $TYPE$
            ORBIT = $ORBIT$
            SIZE = $SIZE$
            MIN_ANGLE = $MIN_ANGLE$
            MAX_ANGLE = $MAX_ANGLE$
        }
    }
    else_if = {
        limit = { check_variable_arithmetic = { which = tOrbitOffset value < 20 } }
        inline_script = {
            script = galactic_objects/mpp_i_s_go_orbit_offset_0_10
            B_OFFSET = 10
            TYPE = $TYPE$
            ORBIT = $ORBIT$
            SIZE = $SIZE$
            MIN_ANGLE = $MIN_ANGLE$
            MAX_ANGLE = $MAX_ANGLE$
        }
    }
    else_if = {
        limit = { check_variable_arithmetic = { which = tOrbitOffset value < 30 } }
        inline_script = {
            script = galactic_objects/mpp_i_s_go_orbit_offset_0_10
            B_OFFSET = 20
            TYPE = $TYPE$
            ORBIT = $ORBIT$
            SIZE = $SIZE$
            MIN_ANGLE = $MIN_ANGLE$
            MAX_ANGLE = $MAX_ANGLE$
        }
    }
    else_if = {
        limit = { check_variable_arithmetic = { which = tOrbitOffset value < 40 } }
        inline_script = {
            script = galactic_objects/mpp_i_s_go_orbit_offset_0_10
            B_OFFSET = 30
            TYPE = $TYPE$
            ORBIT = $ORBIT$
            SIZE = $SIZE$
            MIN_ANGLE = $MIN_ANGLE$
            MAX_ANGLE = $MAX_ANGLE$
        }
    }
    else_if = {
        limit = { check_variable_arithmetic = { which = tOrbitOffset value < 50 } }
        inline_script = {
            script = galactic_objects/mpp_i_s_go_orbit_offset_0_10
            B_OFFSET = 40
            TYPE = $TYPE$
            ORBIT = $ORBIT$
            SIZE = $SIZE$
            MIN_ANGLE = $MIN_ANGLE$
            MAX_ANGLE = $MAX_ANGLE$
        }
    }
}
else = {
    if = {
        limit = { check_variable_arithmetic = { which = tOrbitOffset value >= -10 } }
        inline_script = {
            script = galactic_objects/mpp_i_s_go_orbit_offset_0_10
            B_OFFSET = -10
            TYPE = $TYPE$
            ORBIT = $ORBIT$
            SIZE = $SIZE$
            MIN_ANGLE = $MIN_ANGLE$
            MAX_ANGLE = $MAX_ANGLE$
        }
    }
    else_if = {
        limit = { check_variable_arithmetic = { which = tOrbitOffset value >= -20 } }
        inline_script = {
            script = galactic_objects/mpp_i_s_go_orbit_offset_0_10
            B_OFFSET = -20
            TYPE = $TYPE$
            ORBIT = $ORBIT$
            SIZE = $SIZE$
            MIN_ANGLE = $MIN_ANGLE$
            MAX_ANGLE = $MAX_ANGLE$
        }
    }
    else_if = {
        limit = { check_variable_arithmetic = { which = tOrbitOffset value >= -30 } }
        inline_script = {
            script = galactic_objects/mpp_i_s_go_orbit_offset_0_10
            B_OFFSET = -30
            TYPE = $TYPE$
            ORBIT = $ORBIT$
            SIZE = $SIZE$
            MIN_ANGLE = $MIN_ANGLE$
            MAX_ANGLE = $MAX_ANGLE$
        }
    }
    else_if = {
        limit = { check_variable_arithmetic = { which = tOrbitOffset value >= -40 } }
        inline_script = {
            script = galactic_objects/mpp_i_s_go_orbit_offset_0_10
            B_OFFSET = -40
            TYPE = $TYPE$
            ORBIT = $ORBIT$
            SIZE = $SIZE$
            MIN_ANGLE = $MIN_ANGLE$
            MAX_ANGLE = $MAX_ANGLE$
        }
    }
    else_if = {
        limit = { check_variable_arithmetic = { which = tOrbitOffset value >= -50 } }
        inline_script = {
            script = galactic_objects/mpp_i_s_go_orbit_offset_0_10
            B_OFFSET = -50
            TYPE = $TYPE$
            ORBIT = $ORBIT$
            SIZE = $SIZE$
            MIN_ANGLE = $MIN_ANGLE$
            MAX_ANGLE = $MAX_ANGLE$
        }
    }
}

clear_variable = tOrbitOffset
clear_variable = tOffsetMin
clear_variable = tOffsetMax