if = {
    inline_script = {
        script = "galactic_objects/mpp_i_s_g_o_set_location_base"
        F_DISTANCE = @[$B_DISTANCE$ + 0]
    }
}
else_if = {
    inline_script = {
        script = "galactic_objects/mpp_i_s_g_o_set_location_base"
        F_DISTANCE = @[$B_DISTANCE$ + 1]
    }
}
else_if = {
    inline_script = {
        script = "galactic_objects/mpp_i_s_g_o_set_location_base"
        F_DISTANCE = @[$B_DISTANCE$ + 2]
    }
}
else_if = {
    inline_script = {
        script = "galactic_objects/mpp_i_s_g_o_set_location_base"
        F_DISTANCE = @[$B_DISTANCE$ + 3]
    }
}
else_if = {
    inline_script = {
        script = "galactic_objects/mpp_i_s_g_o_set_location_base"
        F_DISTANCE = @[$B_DISTANCE$ + 4]
    }
}
else_if = {
    inline_script = {
        script = "galactic_objects/mpp_i_s_g_o_set_location_base"
        F_DISTANCE = @[$B_DISTANCE$ + 5]
    }
}
else_if = {
    inline_script = {
        script = "galactic_objects/mpp_i_s_g_o_set_location_base"
        F_DISTANCE = @[$B_DISTANCE$ + 6]
    }
}
else_if = {
    inline_script = {
        script = "galactic_objects/mpp_i_s_g_o_set_location_base"
        F_DISTANCE = @[$B_DISTANCE$ + 7]
    }
}
else_if = {
    inline_script = {
        script = "galactic_objects/mpp_i_s_g_o_set_location_base"
        F_DISTANCE = @[$B_DISTANCE$ + 8]
    }
}
else_if = {
    inline_script = {
        script = "galactic_objects/mpp_i_s_g_o_set_location_base"
        F_DISTANCE = @[$B_DISTANCE$ + 9]
    }
}
else_if = {
    inline_script = {
        script = "galactic_objects/mpp_i_s_g_o_set_location_base"
        F_DISTANCE = @[$B_DISTANCE$ + 10]
    }
}