set_variable = { which = tLocation value = $LOCATION$ }
set_variable = { which = inOrbit value = $IN_ORBIT$ }

if = {
    limit = { NOT = { check_variable = { which = tLocation value = @mpp_no } } }
    # LOCATION definido
    if = {
        limit = { check_variable = { which = tLocation value = @mpp_prev } }
        # IN ORBIT
        if = {
            limit = { check_variable = { which = inOrbit value = @mpp_yes } }
            create_ambient_object = {
                type = $TYPE$
                location = prev
                entity_offset = $LOCATION_DISTANCE$
                entity_offset_height = -10

                # TODO añadir cálculo de angle
                entity_offset_angle = { min = 0 max = 359 }
            }
            # ---------- Debug ----------
            mpp_log_info_ao = {
                EFFECT = $EFFECT$ TEXT = "create_ambient_object $TYPE$ en prev a $LOCATION_DISTANCE$ inOrbt"
            }
        }
        else = {
            create_ambient_object = {
                type = $TYPE$
                location = prev
                entity_offset = $LOCATION_DISTANCE$

                # TODO añadir cálculo de angle
                entity_offset_angle = { min = 0 max = 359 }
            }
            # ---------- Debug ----------
            mpp_log_info_ao = {
                EFFECT = $EFFECT$ TEXT = "create_ambient_object $TYPE$ en prev a $LOCATION_DISTANCE$"
            }
        }
    }
    else_if = {
        limit = { check_variable = { which = tLocation value = @mpp_from } }
        # IN ORBIT
        if = {
            limit = { check_variable = { which = inOrbit value = @mpp_yes } }
            create_ambient_object = {
                type = $TYPE$
                location = from
                entity_offset = $LOCATION_DISTANCE$
                entity_offset_height = -10

                # TODO añadir cálculo de angle
                entity_offset_angle = { min = 0 max = 359 }
            }
            # ---------- Debug ----------
            mpp_log_info_ao = {
                EFFECT = $EFFECT$ TEXT = "create_ambient_object $TYPE$ en from a $LOCATION_DISTANCE$ inOrbt"
            }
        }
        else = {
            create_ambient_object = {
                type = $TYPE$
                location = from
                entity_offset = $LOCATION_DISTANCE$

                # TODO añadir cálculo de angle
                entity_offset_angle = { min = 0 max = 359 }
            }
            # ---------- Debug ----------
            mpp_log_info_ao = {
                EFFECT = $EFFECT$ TEXT = "create_ambient_object $TYPE$ en from a $LOCATION_DISTANCE$"
            }
        }
    }
    else_if = {
        limit = { check_variable = { which = tLocation value = @mpp_prev2 } }
        # IN ORBIT
        if = {
            limit = { check_variable = { which = inOrbit value = @mpp_yes } }
            create_ambient_object = {
                type = $TYPE$
                location = prevprev
                entity_offset = $LOCATION_DISTANCE$
                entity_offset_height = -10

                # TODO añadir cálculo de angle
                entity_offset_angle = { min = 0 max = 359 }
            }
            # ---------- Debug ----------
            mpp_log_info_ao = {
                EFFECT = $EFFECT$ TEXT = "create_ambient_object $TYPE$ en prevprev a $LOCATION_DISTANCE$ inOrbt"
            }
        }
        else = {
            create_ambient_object = {
                type = $TYPE$
                location = prevprev
                entity_offset = $LOCATION_DISTANCE$

                # TODO añadir cálculo de angle
                entity_offset_angle = { min = 0 max = 359 }
            }
            # ---------- Debug ----------
            mpp_log_info_ao = {
                EFFECT = $EFFECT$ TEXT = "create_ambient_object $TYPE$ en prevprev a $LOCATION_DISTANCE$"
            }
        }
    }
    else = {
        # ---------- Debug ----------
        mpp_log_effect_error = {
            EFFECT = $EFFECT$ TEXT = "LOCATION $LOCATION$ no admitido para create_ambient_object"
        }
    }
}
else_if = {
    limit = { exists = prev }
    # IN ORBIT
    if = {
        limit = { check_variable = { which = inOrbit value = @mpp_yes } }
        create_ambient_object = {
            type = $TYPE$
            location = prev
            entity_offset = $LOCATION_DISTANCE$
            entity_offset_height = -10

            # TODO añadir cálculo de angle
            entity_offset_angle = { min = 0 max = 359 }
        }
        # ---------- Debug ----------
        mpp_log_info_ao = {
            EFFECT = $EFFECT$ TEXT = "create_ambient_object $TYPE$ en prev a $LOCATION_DISTANCE$ inOrbt"
        }
    }
    else = {
        create_ambient_object = {
            type = $TYPE$
            location = prev
            entity_offset = $LOCATION_DISTANCE$

            # TODO añadir cálculo de angle
            entity_offset_angle = { min = 0 max = 359 }
        }
        # ---------- Debug ----------
        mpp_log_info_ao = {
            EFFECT = $EFFECT$ TEXT = "create_ambient_object $TYPE$ en prev a $LOCATION_DISTANCE$"
        }
    }
}
else_if = {
    limit = { exists = from }
    # IN ORBIT
    if = {
        limit = { check_variable = { which = inOrbit value = @mpp_yes } }
        create_ambient_object = {
            type = $TYPE$
            location = from
            entity_offset = $LOCATION_DISTANCE$
            entity_offset_height = -10

            # TODO añadir cálculo de angle
            entity_offset_angle = { min = 0 max = 359 }
        }
        # ---------- Debug ----------
        mpp_log_info_ao = {
            EFFECT = $EFFECT$ TEXT = "create_ambient_object $TYPE$ en from a $LOCATION_DISTANCE$ inOrbt"
        }
    }
    else = {
        create_ambient_object = {
            type = $TYPE$
            location = from
            entity_offset = $LOCATION_DISTANCE$

            # TODO añadir cálculo de angle
            entity_offset_angle = { min = 0 max = 359 }
        }
        # ---------- Debug ----------
        mpp_log_info_ao = {
            EFFECT = $EFFECT$ TEXT = "create_ambient_object $TYPE$ en from a $LOCATION_DISTANCE$"
        }
    }
}
else = {
    # IN ORBIT
    if = {
        limit = { check_variable = { which = inOrbit value = @mpp_yes } }
        create_ambient_object = {
            type = $TYPE$
            location = this
            entity_offset = $LOCATION_DISTANCE$
            entity_offset_height = -10

            # TODO añadir cálculo de angle
            entity_offset_angle = { min = 0 max = 359 }
        }
        # ---------- Debug ----------
        mpp_log_info_ao = {
            EFFECT = $EFFECT$ TEXT = "create_ambient_object $TYPE$ en this a $LOCATION_DISTANCE$ inOrbt"
        }
    }
    else = {
        create_ambient_object = {
            type = $TYPE$
            location = this
            entity_offset = $LOCATION_DISTANCE$

            # TODO añadir cálculo de angle
            entity_offset_angle = { min = 0 max = 359 }
        }
        # ---------- Debug ----------
        mpp_log_info_ao = {
            EFFECT = $EFFECT$ TEXT = "create_ambient_object $TYPE$ en this a $LOCATION_DISTANCE$"
        }
    }
}

# ---------- Limpieza de variables ----------
clear_variable = tLocation
clear_variable = inOrbit

## create_ambient_object
# type = <ambient_object>
# location = scope_group[spatial_object]
# scale = float
# use_3d_location = bool
# entity_offset = float
# entity_offset = { min = float max = float }
# entity_offset_angle = int
# entity_offset_angle = { min = int max = int }
# entity_offset_height = float
# entity_offset_height = { min = float max = float }
# base_angle_towards = scope[country]
# base_angle_towards = scope_group[spatial_object]
# entity_face_object = scope_group[spatial_object]
# entity_scale_to_size = bool
# scripted_scale = value[variable]
# play_animation_once = bool
# duration = int
# effect = single_alias_right[effect_clause]