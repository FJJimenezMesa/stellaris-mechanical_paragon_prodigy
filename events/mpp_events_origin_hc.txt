# -----------------------
# NAMESPACE
# -----------------------
namespace = mpp_events_origin_hc

@countDownDays_noAtHome_1 = 5
@countDownDays_noAtHome_2 = { 365*2 }
@countDownDays_noAtHome_3 = { 365*3 }

# ===================================================
# Chain: Home Comming
# ===================================================
country_event = {
    id = mpp_events_origin_hc.0
    title = mpp_events_origin_hc.0.title
    desc = mpp_events_origin_hc.0.desc
    #picture = GFX_evt_ruined_city # TODO crear/buscar uno
    #show_sound = event_planetary_riot
    is_triggered_only = yes
    hide_window = yes

    immediate = {
        # Chain de eventos narrativos tras perder/no estar en casa
        # TODO Encapsular listado de eventos? mover a otro archivo? 
        country_event = { id = mpp_events_origin_hc.1 days = @countDownDays_noAtHome_1 } 
        country_event = { id = mpp_events_origin_hc.2 days = @countDownDays_noAtHome_2 }    
        country_event = { id = mpp_events_origin_hc.3 days = @countDownDays_noAtHome_3 }
    }
    option = {
        name = mpp_events_origin_hc.0.a
        # TODO efectos de la opción inicial
    }
}

country_event = {
    id = mpp_events_origin_hc.1
    title = mpp_events_origin_hc.1.title
    desc = mpp_events_origin_hc.1.desc
    is_triggered_only = yes
    hide_window = no

    immediate = {
        # TODO Evento narrativo avisando de que va a pasar tras perder/no estar en casa
    }
    option = {
        name = mpp_events_origin_hc.1.a
        # TODO efectos
    }
}

country_event = {
    id = mpp_events_origin_hc.2
    title = mpp_events_origin_hc.2.title
    desc = mpp_events_origin_hc.2.desc
    is_triggered_only = yes
    hide_window = no

    immediate = {
        # TODO Evento narrativo avisando de que va a pasar tras perder/no estar en casa
    }
    option = {
        name = mpp_events_origin_hc.2.a
        # TODO efectos
    }
}

country_event = {
    id = mpp_events_origin_hc.3
    title = mpp_events_origin_hc.3.title
    desc = mpp_events_origin_hc.3.desc
    is_triggered_only = yes
    hide_window = no

    immediate = {
        # TODO Evento narrativo avisando de que va a pasar tras perder/no estar en casa
    }
    option = {
        name = mpp_events_origin_hc.3.a
        # TODO efectos
    }
}
